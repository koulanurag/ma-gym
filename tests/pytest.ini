[pytest]
addopts =
    -v
    --no-success-flaky-report
    --tb=native

testpaths = tests/
python_functions = test_*
python_paths = ./

norecursedirs =
    .git
    archive
    bin
    build
    dist
    locals